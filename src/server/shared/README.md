# server/shared

前后端共用的协议类型。**前端唯一允许 import 的后端目录。**

前端不内联任何 git 知识（状态位含义、空树哈希、路径转义规则、重命名判定），一律由本目录的协议类型承载（架构边界不变式 4）。类型在 S1 随 server 一同定型——包括 S3b/S4 才填充取值的 `watch` / `binary` / `too-large` 分支，字段必须先有。
